package com.lazydev.stksongbook.webapp.api.restcontroller;

import com.lazydev.stksongbook.webapp.api.dto.UserRoleDTO;
import com.lazydev.stksongbook.webapp.data.model.UserRole;
import org.junit.Test;
import org.mockito.Mockito;

import static org.junit.Assert.assertEquals;

public class UserRoleRestControllerTest {
/*
    @Test
    public void whenConvertUserRoleEntityToUserRoleDTO_thenCorrect() {
        UserRole userRole = new UserRole();
        userRole.setId(1L);
        userRole.setName("test");
        UserRoleRestController restController = Mockito.mock(UserRoleRestController.class);

        UserRoleDTO userRoleDto = restController.convertToDto(userRole);

        assertEquals(userRole.getId(), userRoleDto.getId());
        assertEquals(userRole.getName(), userRoleDto.getName());
    }

    @Test
    public void whenConvertUserRoleDTOToUserRoleEntity_thenCorrect() {
        UserRoleDTO userRoleDto = new UserRoleDTO();
        userRoleDto.setId(1L);
        userRoleDto.setName("test");
        UserRoleRestController restController = Mockito.mock(UserRoleRestController.class);

        UserRole userRole = restController.convertToEntity(userRoleDto);
        assertEquals(userRoleDto.getId(), userRole.getId());
        assertEquals(userRoleDto.getName(), userRole.getName());
    }*/
}